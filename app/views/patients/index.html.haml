= render partial: "index_header"
.container-fluid
  .row
    .col
      - if @patients.size > 0
        = render partial: "index_list"
        = paginate @patients
      - else
        %p
          No patients yet.
